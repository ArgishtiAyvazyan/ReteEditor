import { ClassicPreset } from "rete";

const socket = new ClassicPreset.Socket('socket');

console.log(socket)

export default socket


/*import Rete from "rete";

const numSocket = new Rete.Socket('Number value');
const image = new Rete.Socket('Image')


export default {
    numSocket,
    image
}*/